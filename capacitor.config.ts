import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.motorcycleverify.app',
  appName: 'Motorcycle Verification Platform',
  webDir: 'dist',
}

export default config
